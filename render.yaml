services:
  # 1. The Express Backend API (Web Service Server)
  - type: web
    name: lifevault-api
    env: node
    rootDir: api
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
      - key: MONGODB_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true

  # 2. The Vite React Frontend (Static CDN Site)
  - type: web
    name: lifevault-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_BACKEND_URL
        value: https://lifevault-api.onrender.com # Directly maps to backend's external URL domain path